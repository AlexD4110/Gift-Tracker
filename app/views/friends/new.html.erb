<div class="container mt-5">
  <div class="card">
    <div class="card-body">
      <h1 class="card-title">New Friend</h1>
      <%= render 'form', friend: @friend %>

      <div class="mt-4">
        <%= link_to "Back to friends", friends_path, class: 'btn btn-secondary' %>
      </div>
    </div>
  </div>
</div>